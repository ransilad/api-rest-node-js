{
    "development": {
        "MONGO_URI": "mongodb://localhost:27017/tvshow",
        "MONGO_OPTIONS": { "db": { "safe": true } }
    },
    "production": {
        "MONGO_URI": "mongodb://user:password@server:port/db",
        "MONGO_OPTIONS": { "db": { "safe": true } }
    },
    "sandbox": {
        "MONGO_URI": "mongodb://user:password@server:port/db",
        "MONGO_OPTIONS": { "db": { "safe": true } }
    }
}